name: CI

on:
  push:
    branches:
      - main
      - 'dependabot/**'
  pull_request:
    branches:
      - '**'

jobs:
  test:
    name: Lint and test

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get Node.js version from package.json
        run: |
          node -p 'require("./package.json").engines.node' | tee /tmp/node-version

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version-file: /tmp/node-version
          cache: npm

      - name: Install latest npm
        run: npm install --global npm@latest

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test
