name: CI

on:
  push:
    branches:
      - main
      - 'dependabot/**'
  pull_request:
    branches:
      - '**'

jobs:
  test:
    uses: stylelint/.github/.github/workflows/test.yml@reusable-workflow
    with:
      node-version: '["16"]'
